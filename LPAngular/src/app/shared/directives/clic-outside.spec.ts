import { ClicOutside } from './clic-outside';

describe('ClicOutside', () => {
  it('should create an instance', () => {
    const directive = new ClicOutside();
    expect(directive).toBeTruthy();
  });
});
